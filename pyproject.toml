[project]
name = "financing-agent-sdk"
version = "0.1.0"
description = "SDK for analyzing SMB financing offers"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
]

[project.scripts]
financing-agent = "financing_sdk.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
